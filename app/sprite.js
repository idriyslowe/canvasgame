/* global angular */
angular.module("spellSword").constant("spriteBuilder", {
  cowboy: 10,
  punk: 20
  // put the references to all of your sprites in here... 
  // name them based on their look... 
  // they're numbered in order. why number them? are we referencing by number?
});